<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Enhanced shadow layer -->
    <item android:drawable="@drawable/card_shadow_small"/>
    
    <!-- Main card with gradient -->
    <item
        android:bottom="6dp"
        android:right="6dp"
        android:top="0dp"
        android:left="0dp">
        <shape android:shape="rectangle">
            <gradient
                android:angle="135"
                android:startColor="@color/card_gradient_start"
                android:centerColor="@color/card_gradient_mid"
                android:endColor="@color/card_gradient_end"
                android:type="linear" />
            <corners android:radius="16dp"/>
        </shape>
    </item>
    
    <!-- Subtle border -->
    <item
        android:bottom="6dp"
        android:right="6dp"
        android:top="0dp"
        android:left="0dp">
        <shape android:shape="rectangle">
            <stroke
                android:width="0.5dp"
                android:color="@color/md_theme_dark_outlineVariant" />
            <corners android:radius="16dp"/>
        </shape>
    </item>
</layer-list>
