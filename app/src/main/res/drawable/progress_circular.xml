<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background">
        <shape
            android:shape="ring"
            android:innerRadiusRatio="2.5"
            android:thickness="16dp"
            android:useLevel="false">
            <solid android:color="@color/md_theme_dark_surfaceVariant"/>
        </shape>
    </item>
    <item android:id="@android:id/progress">
        <rotate
            android:fromDegrees="270"
            android:toDegrees="270">
            <shape
                android:shape="ring"
                android:innerRadiusRatio="2.5"
                android:thickness="16dp"
                android:useLevel="true">
                <gradient
                    android:angle="0"
                    android:startColor="@color/gradient_start_cosmic"
                    android:centerColor="@color/gradient_end_sunrise"
                    android:endColor="@color/gradient_golden"
                    android:type="sweep"/>
            </shape>
        </rotate>
    </item>
</layer-list>
